import {icon,esc} from './components.js';
export function renderLogin(message='',busy=false){return `<div class="access-page"><section class="access-card"><a class="wordmark" href="#/vandaag">AVARYN</a><p class="eyebrow">Je paarden dichtbij</p><h1>Welkom terug.</h1><p>Log in met je fictieve testaccount om samen verder te kijken.</p>${message?`<p class="form-error" role="alert">${esc(message)}</p>`:''}<form id="login-form"><label class="form-field">E-mailadres<input type="email" name="email" autocomplete="username" required></label><label class="form-field">Wachtwoord<input type="password" name="password" autocomplete="current-password" required></label><button class="button-primary" type="submit"${busy?' disabled':''}>${icon('arrow-right',17)} ${busy?'Even verbinden…':'Inloggen'}</button></form><button class="text-link" data-action="enter-demo">Bekijk het visuele voorbeeld ${icon('chevron-right',15)}</button><p class="access-note">Dit is de besloten testomgeving. Gebruik je testaccount voor de dagelijkse zorg en planning. Momenten en abonnementen zijn productvoorstellen.</p></section><div class="access-photo"><img src="assets/orion.png" alt="Paard in het AVARYN-voorbeeld"><p>Rust in je dag.<br>Ruimte voor je paard.</p></div></div>`;}
export function prototypeNotice(){return `<aside class="prototype-notice">${icon('info',18)}<p><strong>Prototype</strong> Je probeert dit onderdeel lokaal uit. Wijzigingen worden nog niet gedeeld met het stalteam.</p></aside>`;}
export function backendNotice(state){return `<div class="connection-context"><span>${icon('stable',17)} ${esc(state.stableName)}</span><button class="text-link" data-action="switch-stable">Stal kiezen ${icon('chevron-down',15)}</button></div>`;}
export function loginPending(){return `<div class="access-loading" role="status"><span class="wordmark">AVARYN</span><h1>Je dag wordt klaargezet.</h1><p>Je paarden, afspraken en stalgegevens worden opgehaald.</p></div>`;}
